# PayU TEST credentials — from PayU Dashboard → Test Mode → Key Salt Details
# (real .env.payu is gitignored — never commit credentials)
NPCI_MODE=payu
PAYU_MERCHANT_KEY=gbin5F
PAYU_SALT=ZkQ62Vf9ZXIHLcqPVUAx0zGxOdQaoz9o
PAYU_BASE_URL=https://test.payu.in
# PUBLIC_BASE_URL=https://aasthi-chain.vercel.app
